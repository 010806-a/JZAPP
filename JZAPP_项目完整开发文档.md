# JZAPP（MoneyBook）项目完整开发文档

> 本文档基于对 `JZ_backup_20260919_065806.zip` 全量代码（82 个 Kotlin 源文件，约 27,800 行；117 个资源文件）的逐文件通读整理而成，覆盖全部类、全部资源与整体架构，供后续开发、AI 辅助编程（AGENTS.md / GEMINI.md）与新成员上手参考。
>
> 生成时间：2026-09-19　代码快照：`JZ_backup_20260919_065806.zip`

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈与构建配置](#2-技术栈与构建配置)
3. [项目目录结构](#3-项目目录结构)
4. [整体架构与数据流](#4-整体架构与数据流)
5. [数据层详解](#5-数据层详解)
6. [账单导入 / 导出体系](#6-账单导入--导出体系)
7. [工具类（utils）](#7-工具类utils)
8. [UI 层详解（按功能模块）](#8-ui-层详解按功能模块)
9. [资源文件概览](#9-资源文件概览)
10. [安全机制说明](#10-安全机制说明)
11. [已知问题 / 技术债 / 未完成占位](#11-已知问题--技术债--未完成占位)
12. [后续开发建议](#12-后续开发建议)
13. [全量类清单速查表](#13-全量类清单速查表)

---

## 1. 项目概述

**MoneyBook（内部包名 `com.example.myno.jz`）** 是一款**完全离线**的 Android 个人记账应用，核心特性：

- 手动记账（支出 / 收入）、账户管理、转账、预算管理、分类管理；
- 支持导入微信 / 支付宝等平台的账单导出文件（CSV / XLSX），自动识别收入、支出、"中性交易"（转账、提现、充值等），并进行去重与账户自动匹配；
- 支持将账单导出为 XLSX（完全自研 OOXML 生成，不依赖 Apache POI 等第三方库）；
- 本地 JSON 全量备份与恢复；
- 应用隐私锁（PIN 密码 / 图案锁，PBKDF2 存储）；
- 邮箱验证机制（用于修改隐私锁 / 找回密码等敏感操作的二次验证，基于 Jakarta Mail 实现 IMAP/SMTP 测试与验证码发送，Android Keystore 加密保存邮箱授权码）；
- 首页快捷入口可自定义排序（支持拖拽排序 + 显示/隐藏）；
- 全局崩溃捕获与本地日志系统。

应用没有账号体系、没有云同步、没有网络请求依赖（仅邮箱验证功能使用网络），所有数据都以 JSON 文件 / SharedPreferences / Android Keystore 形式保存在设备本地。

---

## 2. 技术栈与构建配置

| 项目 | 取值 |
|---|---|
| 语言 | Kotlin 2.1.0 |
| Android Gradle Plugin | 8.13.0 |
| compileSdk | 36 |
| minSdk | 23 |
| targetSdk | 34 |
| applicationId / namespace | `com.example.myno.jz` |
| UI 方案 | 传统 View + XML 布局 + **ViewBinding**（未使用 Jetpack Compose） |
| 架构 | 轻量 MVVM（`AndroidViewModel` + `LiveData`），无 Room、无 DI 框架（无 Hilt/Koin），仓库类手动 `new` |
| 持久化 | **JSON 文件**（`Gson`，保存于 `context.filesDir`）+ `SharedPreferences`（配置/安全数据）+ `AndroidKeyStore`（邮箱授权码加密） |
| 图表 | `com.github.PhilJay:MPAndroidChart:v3.1.0`（饼图 + 趋势折线/柱状图） |
| 邮件 | `org.eclipse.angus:jakarta.mail:2.0.3`（IMAP/SMTP 连接测试与验证码发送） |
| JSON | `com.google.code.gson:gson` |
| 其他 AndroidX | core-ktx / appcompat / material(3) / constraintlayout / activity-ktx / fragment-ktx / lifecycle-*-ktx / recyclerview |

### 关键 Gradle 配置片段（`app/build.gradle.kts`）

```kotlin
android {
    namespace = "com.example.myno.jz"
    compileSdk = 36
    defaultConfig {
        applicationId = "com.example.myno.jz"
        minSdk = 23
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }
    buildFeatures { viewBinding = true }
    compileOptions { sourceCompatibility/targetCompatibility = JavaVersion.VERSION_17 }
}
```

> `build.gradle.kts` 顶部注释「Plugins automatically generated by MLGradleWriter.kt」表明该工程通过 **AndroidIDE** 一类的移动端 IDE 工具链生成/维护 —— 与开发者的移动端开发环境（AndroidIDE、Trae AI）相符。

### AndroidManifest 要点

- `MoneyBookApplication`：应用入口，`onCreate()` 中初始化 `AppLogger` 与 `CrashHandler`。
- 仅声明了 `INTERNET` 权限（邮箱验证功能需要）。
- 仅两个 `<activity>` 被注册：
  - `ui.main.MainActivity`（`exported=true`，`LAUNCHER`，并挂载 `@xml/shortcuts` 长按桌面快捷方式）
  - `ui.quick.QuickEntryActivity`（`exported=false`，供桌面快捷方式「快速记一笔」使用）
- **`android:allowBackup="false"`**：禁止系统级自动备份/云端备份，所有备份均通过应用内置的 `BackupRepository` 完成。

---

## 3. 项目目录结构

```
app/src/main/
├── AndroidManifest.xml
├── kotlin/com/example/myno/jz/
│   ├── MoneyBookApplication.kt          # Application 入口
│   ├── data/
│   │   ├── model/          # 11 个数据模型（纯 data class / enum，无注解）
│   │   ├── local/           # JsonDataStore（通用 JSON CRUD）、DefaultDataInitializer（默认数据播种）
│   │   ├── repository/      # FinanceRepository / BackupRepository / PrivacyLockStore /
│   │   │                    # EmailConfigStore / EmailCredentialStore / EmailVerificationStore
│   │   ├── imports/         # CsvBillParser / XlsxBillParser / ImportBillItem / ImportDuplicateChecker
│   │   └── email/           # EmailConnectionTester（IMAP/SMTP 测试 + 验证码发送）
│   ├── ui/
│   │   ├── main/            # MainActivity（底部导航 + 隐私锁拦截）、MainViewModel
│   │   ├── home/            # 首页：余额卡片、快捷入口管理、月度预算预览
│   │   ├── bills/           # 账单列表/详情/新增/导入预览（本项目中最复杂的模块）
│   │   ├── assets/          # 账户（资产）管理、账户流水、转账
│   │   ├── budget/          # 预算管理
│   │   ├── category/        # 分类管理
│   │   ├── statistics/      # 统计（饼图 + 趋势图）
│   │   ├── backup/          # 本地备份/恢复
│   │   ├── email/           # 邮箱配置 + 邮箱验证码校验
│   │   ├── privacy/         # 隐私锁（设置/校验/修改/重置，PIN + 图案锁）
│   │   ├── settings/        # 应用设置（主题、动画、默认账户/分类等）
│   │   ├── mine/            # 「我的」页面（功能入口聚合）
│   │   ├── quick/           # QuickEntryActivity（桌面快捷方式承载）
│   │   ├── common/          # FragmentNavigation（Fragment 导航扩展函数）
│   │   └── account/, budget/BudgetActivity.kt, category/CategoryActivity.kt,
│   │       backup/BackupActivity.kt, importbill/, addbill/, lock/LockActivity.kt
│   │       # 以上均为【空占位类】，未接入 Manifest，见第 11 节
│   └── utils/
│       ├── AppLogger.kt          # 文件日志系统
│       ├── CrashHandler.kt       # 全局未捕获异常处理
│       ├── ExcelExporter.kt      # 自研 XLSX 导出（最大文件，1306 行）
│       ├── FinanceCalculator.kt  # 日期区间 & 收支统计计算
│       ├── BackupManager.kt      # 【空占位】
│       └── NaturalLanguageParser.kt # 【空占位，自然语言记账未实现】
└── res/
    ├── layout/     40 个布局（Activity 3 + Fragment 29 + Item 8）
    ├── values(-night)/  colors / strings / styles / themes
    ├── mipmap-*/   应用图标
    ├── drawable(-v24)/  51 个图标资源
    └── xml/        backup_rules, data_extraction_rules, shortcuts
```

---

## 4. 整体架构与数据流

```
                     ┌─────────────────────┐
                     │     MainActivity     │  底部导航宿主 + 隐私锁拦截（onResume 检查）
                     └──────────┬──────────┘
                                │ replace(fragmentContainer, ...)
        ┌───────────┬──────────┼──────────┬────────────┐
        ▼           ▼          ▼          ▼            ▼
   HomeFragment BillsFragment StatisticsFragment AssetsFragment MineFragment
        │           │                               │             │
        └─────┬─────┴───────────────┬───────────────┘             │
              ▼                     ▼                              ▼
        MainViewModel        （各自 Fragment 内部        BackupFragment / EmailConfigFragment /
     (activityViewModels     直接 new Repository）        PrivacyLockFragment / SettingsFragment /
      跨 Fragment 共享)                                    CategoryManageFragment / BudgetManageFragment
              │
              ▼
      FinanceRepository  ── 封装 ──▶  JsonDataStore  ── 读写 ──▶  filesDir/*.json
              │                                                  (records / accounts /
              │                                                   categories / budgets /
              │                                                   transfers / settings)
              ▼
   DefaultDataInitializer（首次启动播种默认账户与分类）
```

要点：

1. **状态管理不统一**：`MainViewModel`（`activityViewModels`，跨 Home/AddBill 等共享）持有 bills/accounts/categories 的 `LiveData`；但 `BillsFragment`、`AssetsFragment`、`StatisticsFragment`、`BudgetManageFragment` 等大多**各自直接 `FinanceRepository(context)`**，并不统一走 `MainViewModel`，因此存在**多份仓库实例、无跨页面自动刷新机制**——某个页面改了数据，其它已打开页面需要 `onResume`/手动 `refresh()` 才能感知。
2. **无数据库、无事务**：所有列表都是"读整个 JSON 数组 → 内存里增删改 → 整个数组重新写回文件"（见 `JsonDataStore` 的 `readList`/`writeList`），量大时有性能与并发写风险，但目前场景（个人记账、单进程单线程 UI 调用）问题不大。
3. **Fragment 导航**：绝大部分二级页面使用 `FragmentManager.openScreen()`（`ui/common/FragmentNavigation.kt` 的扩展函数）`replace + addToBackStack`，全部塞进 `MainActivity` 的同一个 `R.id.fragmentContainer`；没有使用 Navigation Component。
4. **隐私锁拦截**在 `MainActivity.onResume()` 中检查，一旦命中会强制 `replace` 成 `PrivacyLockVerifyFragment`（不进返回栈），通过 `FragmentResultListener("privacy_lock_verify_result")` 收到验证结果后恢复到验证前的 Tab。

---

## 5. 数据层详解

### 5.1 数据模型（`data/model/`）

| 文件 | 核心类型 | 说明 |
|---|---|---|
| `Bill.kt` | `Bill` / `BillType{EXPENSE,INCOME}` | 账单记录；`source`/`sourceTransactionId` 用于导入去重比对 |
| `Account.kt` | `Account` / `AccountType{BANK_CARD,WECHAT,ALIPAY,CASH,OTHER}` | **`balance` 字段是期初余额，不是当前余额**（当前余额=期初+收入-支出+转入-转出，动态计算，见各 UI 的 `calculateAccountBalance`） |
| `Category.kt` | `Category` / `CategoryType{EXPENSE,INCOME}` | 分类，`isSystem` 标记系统预置分类（禁止删除），`visible` 控制是否显示在快捷记账 |
| `Budget.kt` | `Budget` / `BudgetType{TOTAL,CATEGORY}` | 按年月维度的预算，`warningPercent` 预警阈值（默认 80%） |
| `Transfer.kt` | `Transfer` | 账户间转账，同样带 `source`/`sourceTransactionId` |
| `AppSettings.kt` | `AppSettings` + 7 个枚举（`LockType`/`ThemeMode`/`DefaultBillType`/`AfterAddAction`/`DateFormatType`/`DecimalPlaces`/`StatisticsPeriod`） | 全局应用设置单例，含旧字段 `darkMode`（兼容旧版本数据，实际生效的是 `themeMode`） |
| `EmailConfig.kt` / `EmailProvider.kt` | `EmailConfig` / `EmailProvider{QQ,NETEASE_163,NETEASE_126,SINA,GMAIL,OUTLOOK,CUSTOM}` | 邮箱 IMAP/SMTP 配置及预置服务商参数 |
| `EmailVerification.kt` | `EmailVerification` | 验证码记录结构（实际存储走 `EmailVerificationStore` 的 SharedPreferences，此 data class 未直接用于持久化） |
| `PrivacyLockConfig.kt` | `PrivacyLockConfig` / `PrivacyLockType{NONE,PASSWORD,PATTERN}` | 隐私锁配置（凭证以哈希+盐存储） |
| `BackupData.kt` | `BackupData` | 备份 JSON 顶层结构占位（字段类型为 `Any?`，实际备份逻辑在 `BackupRepository` 中用 Gson `JsonElement` 直接拼装，未真正使用此 data class） |

### 5.2 `JsonDataStore`（`data/local/JsonDataStore.kt`，540 行）

- 6 个 JSON 文件：`records.json`（Bill）、`accounts.json`、`categories.json`、`budgets.json`、`transfers.json`、`settings.json`，均存于 `context.filesDir`。
- 提供**通用泛型 CRUD 模板**：`readList<T>` / `writeList<T>` / `addItem` / `updateItem`（按 `idOf` 匹配替换）/ `deleteItem`；6 类业务对象各自的 `getXxx/saveXxx/addXxx/updateXxx/deleteXxx` 均是这套模板的薄封装。
- 所有读写包 try/catch，异常时返回空列表/默认值并 `printStackTrace()`（**未接入 `AppLogger`**，是与其余模块日志规范不一致的地方）。
- `clearAllData()`：删除全部 6 个 JSON 文件（用于"清空数据"类功能，当前未见 UI 入口调用，属预留能力）。

### 5.3 `DefaultDataInitializer`（250 行）

- App 启动（`MainActivity.onCreate`）时调用 `initialize(context)`；
- 仅当 `accounts.json`/`categories.json` 为空时才写入默认数据，**不会覆盖已有用户数据**；
- 默认账户：现金 / 微信 / 支付宝 / 银行卡（`id` 固定为 `cash`/`wechat`/`alipay`/`bank`，**导入模块会按这些固定 id 做账户自动匹配，删除后重建需注意 id 一致性**）；
- 默认分类：12 个支出分类（餐饮/购物/交通/住房/娱乐/医疗/教育/通讯/宠物/恋爱/人情往来/其他）+ 4 个收入分类（工资/奖金/兼职/其他收入），均 `isSystem = true`。

### 5.4 `FinanceRepository`（225 行）

- 面向 UI 的门面（Facade），内部持有一个 `JsonDataStore`；
- 对 `getBills()`/`getTransfers()` 做了按时间倒序排序，对 `getAccounts()`/`getCategories()` 按 `sortOrder` 升序排序（业务排序放在 Repository 层而非 UI 层，是全项目一致的做法）；
- `createBill()` 会自动生成 UUID、`createdAt`/`updatedAt` 时间戳；其余增删改基本是原样透传。

### 5.5 `BackupRepository`（158 行）

- 备份文件保存在 `filesDir/backup/`，命名规则 `MoneyBook_Backup_yyyyMMdd_HHmmss.json`；
- `createBackup()`：把 6 个 JSON 文件的内容原样组装成一个带 `version`（当前 2）与 `backupTime` 的 JSON 对象整体写出；
- `restoreBackup(file)`：校验 `version` 在 `1..2` 区间（**向后兼容 v1 备份**，v1 只有 `records` 字段，其余保持当前状态不变）；恢复时对每个目标文件走"写临时文件 → 删除旧文件 → rename"的近似原子操作，任一步失败即整体回滚为 `false`（但**多文件之间不是全局事务**，理论上可能出现部分文件恢复成功、部分失败的中间态）；
- `getBackupFiles()` 按最后修改时间倒序列出。

### 5.6 隐私锁 / 邮箱相关存储（均基于 `SharedPreferences`，见第 10 节安全机制）

- `PrivacyLockStore`：PBKDF2WithHmacSHA256（120000 次迭代，256 位）+ 32 字节随机盐，只存哈希，不存明文。
- `EmailConfigStore`：明文存储 IMAP/SMTP 主机、端口、SSL 开关等**非敏感配置**。
- `EmailCredentialStore`：邮箱**授权码**用 `AndroidKeyStore` 生成的 AES 密钥，AES/GCM/NoPadding 加密后存储（IV 与密文分别 Base64 保存）。
- `EmailVerificationStore`：验证码只存 SHA-256 哈希，5 分钟过期（`CODE_EXPIRE_TIME`）、60 秒重发间隔（`RESEND_INTERVAL`）、最多 5 次尝试（`MAX_ATTEMPTS`）。

---

## 6. 账单导入 / 导出体系

这是本项目**技术含量最高、代码量最大**的部分，全部**不依赖第三方 Office 库**，手写 ZIP/XML/OOXML 解析与生成。

### 6.1 导入：`CsvBillParser`（661 行）+ `XlsxBillParser`（955 行）

**共同的业务规则（核心难点在"中性交易"识别）**：

- 微信官方账单表头特征：同时包含"交易时间""交易类型""交易对方""收/支""金额(元)"；命中则走**微信专用解析**（TAB 分隔），否则走**通用 CSV 解析**（自动探测分隔符 `\t`/`,`/`;`，按表头关键字模糊匹配"时间/日期/金额/收支/商户/备注"等列）。
- 微信"收/支"列不是"收入"也不是"支出"时（如"/"），代表**充值、提现、零钱通转入/转出、信用卡还款、账户间转账**等资金内部流转，**不能计入收支统计**：
  - CSV 解析器：直接 `continue` 跳过这类行；
  - XLSX 解析器（更完善）：新增 `ImportBillType.NEUTRAL`，通过 `isTransferTransaction()` 关键字匹配（"转账/转入/转出/提现/充值/零钱通/经营账户"）识别为中性交易并**保留**，交由 `ImportPreviewFragment` 后续按转账处理，而非直接丢弃——**这是 XLSX 解析器相对 CSV 解析器的功能超集，二者逻辑并未完全对齐**（技术债，见第 11 节）。
- 金额解析统一去除 `¥`/`￥`/千分位逗号；时间解析尝试多种 `yyyy-MM-dd[ /]HH:mm[:ss]` 格式；XLSX 额外支持 **Excel 序列日期**（`excelSerialToTimestamp`，以 1899-12-31 为基准，并处理 Excel 1900 年闰年 bug 的 60 天偏移修正）。
- `XlsxBillParser.parse()` 手动用 `ZipInputStream` 解开 xlsx（本质是 zip）取出 `xl/sharedStrings.xml` 与第一个 `xl/worksheets/sheetN.xml`，用 `XmlPullParser` 流式解析出二维字符串表格，再交给 `convertWechatRows()` 做与 CSV 解析器类似的字段级处理，并补充生成 `note`（拼接商品/备注/支付方式/单号等，用 `" · "` 分隔）。

### 6.2 去重：`ImportDuplicateChecker`

- 优先按 `source + transactionId`（platform + 原始交易单号）精确匹配；
- 否则按 `类型 + 金额（容差 0.01）+ 时间（容差 60 秒）+ 商户名`（商户名通过 `note` 中 `" · "` 前缀反推）模糊匹配；
- `NEUTRAL` 类型的导入项**不参与**与已有 `Bill` 的去重比较（因为落地后是 `Transfer` 而非 `Bill`）。

### 6.3 导入预览与落地：`ImportPreviewFragment`（1920 行，全项目单文件最长的逻辑之一）

这是把"解析出的原始条目"转成"真正写入数据库的 Bill/Transfer"的核心枢纽，主要职责：

1. `prepareItems()`：解析 → 去重标记 → 按 UI 展示要求整理；
2. `ensureSystemAccounts()` / `createHiddenSystemAccountIfNeeded()`：为微信/支付宝/银行等来源自动**按需创建隐藏系统账户**，避免用户手动建账户；
3. `prepareTransfers()` + `resolveTransferAccounts()`：对 `NEUTRAL` 类型条目尝试自动推断转出/转入账户，内部按来源分层匹配：
   - `findWechatAccount` / `findAlipayAccount` / `findBusinessAccount`（经营账户）/ `findDailyAccount`（日常消费账户）/ `findBankOrOtherSourceAccount` / `findBankOrOtherTargetAccount` / `findMatchingBankAccount`（通过 `normalizeAccountName` 归一化名称做包含匹配）；
   - `resolveGenericTransferSource` / `resolveGenericTransferTarget` 兜底处理无法精确识别来源的通用转账；
4. `isDuplicateTransfer()`：转账场景下的独立去重逻辑（不同于 `ImportDuplicateChecker`，按账户对+金额+时间近似匹配）；
5. `findCategoryId()`：按商户/备注关键字猜测分类（关键字启发式匹配到已建分类）；
6. `confirmImport()`：批量把勾选（`selected=true`）的条目转换为真正的 `Bill`/`Transfer` 并调用 `FinanceRepository` 落库；
7. `buildBillNote()` / `buildTransferNote()`：生成入库后账单/转账的备注文案。

> 该 Fragment 承担了"解析结果→业务实体"转换的**几乎全部规则引擎**，建议后续重构时将账户/分类自动匹配规则从 UI 层抽到独立的 `ImportResolver`/`UseCase` 类中，便于单测（当前无任何单元测试）。

### 6.4 导出：`ExcelExporter`（1306 行，`utils/ExcelExporter.kt`）

- **完全离线、零第三方依赖**手写生成合法 `.xlsx`（本质是一个包含固定目录结构的 ZIP）：
  - 固定文件：`[Content_Types].xml`、`_rels/.rels`、`docProps/app.xml`、`docProps/core.xml`、`xl/workbook.xml`、`xl/_rels/workbook.xml.rels`、`xl/styles.xml`、`xl/theme/theme1.xml`；
  - 三张业务 Sheet：**账单明细**（`sheet1`）、**统计汇总**（`sheet2`，按分类汇总收支）、**账户汇总**（`sheet3`，各账户余额情况）；
  - `appendStringCell()`/`appendNumberCell()`/`escapeXml()` 等底层工具方法手工拼接符合 OOXML 规范的 `<row>`/`<c>` 单元格 XML。
- 自带 `validate(inputStream)`：反向解析生成好的 XLSX，校验必需 Entry 是否齐全、`sheet1` 是否含数据（检测 `<row r="2">` 是否存在），返回 `ValidationResult`，作为导出功能的自检手段。
- 全流程通过 `AppLogger.i(TAG, ...)` 打了大量埋点日志，便于排查导出失败问题。

---

## 7. 工具类（utils）

| 类 | 行数 | 职责 |
|---|---|---|
| `AppLogger` | 287 | 单例文件日志器：`init(context)` 在 `getExternalFilesDir()/logs/moneybook.log` 建日志文件；`i/w/e/crash` 四级日志，同时输出到 Logcat 与文件；`readLogs()`/`clearLogs()` 支持日志查看与清空（目前 UI 未见暴露日志查看入口，属预留能力） |
| `CrashHandler` | 260 | `install(context)` 挂接 `Thread.setDefaultUncaughtExceptionHandler`；崩溃时收集包名/版本/机型/Android 版本/线程名/异常堆栈并写入 `AppLogger.crash()`，随后转交给系统默认处理器（不吞掉崩溃，保证系统仍会弹出"应用已停止"） |
| `FinanceCalculator` | 355 | 纯函数集合：月初/月末/今日起止时间戳、上一月/下一月、最近 N 个月列表、区间内收入/支出/结余、某时间点前累计结余。全项目的日期与金额统计均复用此类，**无状态、无副作用，非常适合补单元测试** |
| `ExcelExporter` | 1306 | 见第 6.4 节 |
| `BackupManager` | 5 | **空类**（占位，未实现任何逻辑），真正的备份实现在 `data/repository/BackupRepository` |
| `NaturalLanguageParser` | 5 | **空类**（占位），推测原计划支持"自然语言记账"（如输入"今天早饭 15 元"自动解析金额/分类/时间），当前未实现 |

---

## 8. UI 层详解（按功能模块）

### 8.1 `ui/main` — 应用外壳

- **`MainActivity`**（383 行）：`onCreate` 顺序为 应用主题(`applyTheme`) → 隐私锁 Store 初始化 → 注册 `FragmentResultListener` 接收验证结果 → 动画设置 → `DefaultDataInitializer.initialize()` → ViewBinding → `viewModel.refresh()` → 初始化底部导航 → 首次进入显示 `HomeFragment`。`onResume()` 中调用 `checkPrivacyLock()`：若隐私锁开启且本次生命周期未验证过，则记录当前 Tab 并强制切到 `PrivacyLockVerifyFragment`（不进返回栈，杜绝物理返回键绕过锁屏）。
- **`MainViewModel`**：`AndroidViewModel`，持有 `FinanceRepository` 及 bills/accounts/categories 三个 `LiveData`；`addAccount`/`updateAccount`/`deleteAccount` 操作成功后自动 `loadData()` 刷新（**注意：仅账户相关操作会自动刷新 LiveData，Bill/Category/Budget 的增删改并未统一经过本 ViewModel**，多数 Fragment 直接调用自己 new 出来的 `FinanceRepository`）。
- **`ui/common/FragmentNavigation.kt`**：`FragmentManager.openScreen(fragment, addToBackStack=true)` 扩展函数，统一二级页面跳转写法。

### 8.2 `ui/home` — 首页（`HomeFragment` 2031 行，全项目最大的 Fragment）

- 顶部渐变标题："Money"深色 + "Book"品牌蓝拼色（`setupGreeting`，`SpannableString`）；
- **快捷入口宫格**（`allQuickActions`：记一笔/账单统计/分类管理/账单列表/预算管理）：
  - `QuickActionAdapter` + `RecyclerView` 展示，支持**拖拽排序**（`ItemTouchHelper.onMove`）与**滑动隐藏**（`onSwiped`），管理模式 `isQuickActionManagementMode` 下允许编辑；
  - 排序/可见性通过 `QuickActionStore`（独立 JSON 文件 `quick_actions.json`，与 `JsonDataStore` 管理的 6 个业务文件相互独立）持久化；
- **余额卡片可翻转**（`flipBalanceCard`，动画翻面显示"本月收入/支出/结余"）；
- **月份选择器**（`showMonthPicker`）驱动 `updateMonthSummary`/`updateTodaySummary`/`updateBudget`/`updateCategorySummary` 联动刷新；
- **金额可见性开关**（`updateMoneyVisibility`，隐藏时显示 `¥ ****.**` 掩码，属隐私保护小功能）；
- **预算预览**：`updateBudget()` + `updateCategoryBudgetPreview()` + `showCategoryBudgetDialog()`（564 行区间是全文件最庞大的方法块之一，弹窗内直接编辑分类预算）；
- **最近账单列表**：`RecentBillAdapter` 展示 `MainViewModel` 中的 `bills`（取前若干条）。
- 辅助文件：`QuickAction.kt`（数据模型）、`QuickActionStore.kt`（持久化）、`QuickActionAdapter.kt`（RecyclerView 适配器）、`RecentBillAdapter.kt`、`CategoryDonutChartView.kt`（自绘环形图 View，57 行，用于分类占比小图标）。

### 8.3 `ui/bills` — 账单模块（全项目代码量最大的功能域，5331 行）

| 文件 | 行数 | 说明 |
|---|---|---|
| `BillsFragment` | 1350 | 账单列表主页：月份选择、类型筛选（全部/收入/支出）、**精确筛选弹窗**（`showPreciseFilterDialog` + 自定义时间范围 `showFilterTimePicker`）、可拖拽悬浮记账按钮（`moveFab`/`saveFabPosition`/`restoreFabPosition`，位置持久化到 SharedPreferences）、导入入口(`setupImportButton`) |
| `AddBillFragment` | 805 | 新增/编辑账单表单：收支类型切换、分类/账户 Spinner（分别记忆上次选择 `saveCurrentCategorySelection`）、保存后按 `AfterAddAction` 设置决定跳转行为（回首页/回列表/连续记账 `clearInputForNextBill`）、支持快速记账模式（`isQuickEntry`，供 `QuickEntryActivity` 复用） |
| `BillDetailFragment` | 462 | 账单详情查看/编辑/删除 |
| `BillAdapter` | 208 | 账单列表 RecyclerView 适配器，按分类取图标 |
| `ImportBillFragment` | 266 | 导入入口页：文件选择器（CSV/XLSX）、文件解析调度（`parseSelectedFile`），解析完成跳转 `ImportPreviewFragment` |
| `ImportPreviewFragment` | 1920 | 导入结果预览与确认落库，见第 6.3 节详解 |
| `ImportPreviewAdapter` | 310 | 导入预览列表适配器，支持逐条勾选/取消勾选，回调 `onSelectionChanged` |

### 8.4 `ui/assets` — 资产（账户）与转账（3113 行）

| 文件 | 行数 | 说明 |
|---|---|---|
| `AssetsFragment` | 458 | 资产总览：总资产卡片、账户列表（每张卡片动态计算余额 `calculateAccountBalance` = 期初余额+收入-支出+转入-转出） |
| `AccountDetailFragment` | 376 | 单账户详情总览 |
| `AccountFlowFragment` | 810 | 账户流水（按日期分组 `buildGroupedList`/`getDateKey`），点击条目跳转账单/转账详情 |
| `AccountFlowAdapter` / `AccountFlowItem` | 225 / 39 | 流水列表适配器与条目模型 |
| `AccountSettingsFragment` | 447 | 账户编辑（名称/类型/图标/是否计入总资产/启用状态），含删除确认 |
| `AddAccountFragment` | 260 | 新建账户 |
| `TransferFragment` | 187 | 新建转账表单 |
| `EditTransferFragment` | 492 | 编辑转账（含日期/时间选择器） |
| `TransferDetailFragment` | 258 | 转账详情查看/删除 |

### 8.5 `ui/budget` — 预算管理（1399 行）

- `BudgetManageFragment`（835 行）：总预算/分类预算的新增编辑弹窗（`showBudgetDialog`，长达约 480 行，是预算模块最核心的交互）、月份切换、预算总览卡片（`updateBudgetOverview`）；
- `BudgetViewModel`（402 行）：预算数据的 ViewModel 封装；
- `BudgetAdapter`（157 行）：预算列表适配器（展示进度条/预警颜色，依据 `warningPercent`）；
- `BudgetActivity.kt`：**空占位类**，未注册进 Manifest。

### 8.6 `ui/category` — 分类管理（891 行）

- `CategoryManageFragment`（787 行）：支出/收入分类 Tab 切换、拖拽排序（`saveCurrentOrder`）、新增/编辑分类弹窗（`showCategoryDialog`）、删除确认（系统分类禁止删除）；
- `CategoryManageAdapter`（99 行）；
- `CategoryActivity.kt`：**空占位类**。

### 8.7 `ui/statistics` — 统计（`StatisticsFragment`，2039 行，仅次于 `HomeFragment` 的第二大文件）

- 基于 **MPAndroidChart** 实现：
  - **饼图**（`setupPieChart`/`updatePieChart`/`createPieColors`）：当月支出按分类占比；
  - **趋势图**（`setupTrendChart`）：支持**日/月/年**三种周期 Tab（`setupTrendTabs`/`addTrendTab`），分别对应 `updateDailyTrendChart`/`updateMonthlyTrendChart`/`updateYearlyTrendChart`；
  - `updateTrendSummary`/`calculateChangeText`：与上一周期对比的涨跌幅文案；
  - `updateCategoryStatistics`：分类明细列表（金额+占比+图标）。
- 月份选择器与首页/账单模块的实现模式一致（各自独立实现，未抽公共组件——见第 11 节技术债）。

### 8.8 `ui/backup` — 本地备份（718 行）

- `BackupFragment`（630 行）+ `BackupViewModel`（83 行）：备份列表展示、创建备份、恢复备份（含二次确认）、删除备份，均是对 `BackupRepository` 的 UI 封装；
- `BackupActivity.kt`：**空占位类**。

### 8.9 `ui/email` — 邮箱配置与验证（1426 行）

- `EmailConfigFragment`（986 行，全项目第三大文件）：邮箱服务商 Spinner（预置 QQ/163/126/新浪/Gmail/Outlook/自定义，`applyProviderPreset` 自动填充 IMAP/SMTP 主机端口）、查看模式/编辑模式切换（`showViewMode`/`showEditMode`）、连接测试（`testConnection` 调用 `EmailConnectionTester`）、保存配置；
- `EmailVerificationFragment`（440 行）：验证码发送（`sendCode`）与倒计时（`updateCountdown`）、验证码校验（`verifyCode`）；通过 `ACTION_PRIVACY_LOCK`/`ACTION_CHANGE_PASSWORD`/`ACTION_CHANGE_PATTERN` 等常量区分验证后要跳转的敏感操作，验证结果通过 `FragmentResult` 回传给 `MineFragment`。

### 8.10 `ui/privacy` — 隐私锁子系统（1450 行）

| 文件 | 行数 | 说明 |
|---|---|---|
| `PrivacyLockFragment` | 456 | 首次设置隐私锁：密码模式/图案模式切换（`showPasswordMode`/`showPatternMode`），支持关闭隐私锁（需邮箱验证 `disableLockAfterEmailVerification`） |
| `PrivacyLockVerifyFragment` | 419 | 解锁校验页：密码/图案两种校验（`verifyPassword`/`verifyPattern`），忘记密码入口跳转邮箱验证再进重置流程（`openForgotVerification`→`openResetPassword`/`openResetPattern`） |
| `PatternLockView` | 424 | **自绘 View**：3×3 九宫格图案锁，`onDraw` 手动绘制圆点与连线（`drawPoints`/`drawConnectionLines`），触摸事件识别连线路径（`findPoint`） |
| `PasswordSetupHelper` | 61 | 密码保存的公共封装 |
| `PatternSetupHelper` | 88 | 图案保存的公共封装 |
| `ChangeSecurityFragment` | 144 | 修改密码/图案前的入口页（跳邮箱验证） |
| `ChangePasswordFragment` / `ChangePatternFragment` | 76 / 86 | 验证通过后的修改表单 |
| `ResetPasswordFragment` / `ResetPatternFragment` | 76 / 86 | 忘记密码场景下的重置表单 |

### 8.11 `ui/settings` — 应用设置（`SettingsFragment`，555 行）

统一走 `showChoiceDialog()` 单选弹窗模式，可配置项：默认账户/默认分类（`showDefaultAccountDialog`/`showDefaultCategoryDialog`）、主题模式（跟随系统/浅色/深色，`applyTheme`）、动画开关、默认记账类型、记账后跳转行为、日期格式、金额小数位、统计周期。每次修改都调用 `saveSettings(newSettings)` 整体写回 `AppSettings`。

### 8.12 `ui/mine` — 「我的」页面（`MineFragment`，190 行）

功能入口聚合页，`itemXxx.setOnClickListener` 逐一跳转到：备份、统计、隐私锁（先邮箱验证）、修改密码（先邮箱验证）、邮箱配置、分类管理、预算管理、资产（账户）、设置、关于弹窗（`showAbout`）；`itemProfile`/`itemExport`/`itemFeedback` 当前仅 `showComingSoon()` Toast 提示"即将接入"（未实现的预留入口，见第 11 节）。

### 8.13 `ui/quick` — 桌面快捷方式承载

- `QuickEntryActivity`（39 行）：极简 Activity，直接托管 `AddBillFragment.newQuickEntryInstance()`，供长按桌面图标弹出的"快速记一笔"快捷方式（`res/xml/shortcuts.xml`）跳转使用；`onBackPressed` 直接 `finish()`。

### 8.14 空占位 Activity（均未在 AndroidManifest 注册，当前不可达）

`ui/account/AccountActivity.kt`、`ui/budget/BudgetActivity.kt`、`ui/category/CategoryActivity.kt`、`ui/lock/LockActivity.kt`、`ui/backup/BackupActivity.kt`、`ui/importbill/ImportBillActivity.kt`、`ui/addbill/AddBillBottomSheet.kt` —— 均只有 `class Xxx { }` 空壳，无字段无方法，推测是脚手架/命名占位，实际功能均已用同名 `XxxFragment` 实现。

---

## 9. 资源文件概览

- **布局（40 个）**：3 个 Activity 布局（`activity_main`/`activity_quick_entry`/`activity_budget`——注意 `activity_budget.xml` 对应的 `BudgetActivity` 是空类，此布局当前**孤立未被引用**）、29 个 Fragment 布局、8 个 Item（列表条目）布局。
- **values**：
  - `strings.xml`：目前仅登记了应用名与底部导航等寥寥数条字符串，**绝大多数 UI 文案是在 Kotlin 代码中硬编码中文字符串**，未做国际化/字符串资源化；
  - `colors.xml` / `values-night/colors.xml`：品牌色 `primary #4F6EF7`、收入色 `#22A06B`/`#35A66F`、支出色 `#E85D75`/`#E85D5D`（存在两套相近但不完全一致的收支配色定义，需留意统一）；
  - `styles.xml` / `themes.xml`（含夜间版本）。
- **drawable**：51 个图标资源（`drawable` + `drawable-v24`），涵盖分类图标、账户类型图标、功能入口图标等。
- **mipmap-*/**：标准 5 档 DPI 应用图标（含自适应图标 `ic_launcher.xml`）。
- **xml/**：`backup_rules.xml`/`data_extraction_rules.xml`（Android 12+ 数据提取规则，配合 Manifest 中 `allowBackup=false` 使用）、`shortcuts.xml`（桌面长按快捷方式）。
- **menu/bottom_navigation_menu.xml**：底部导航 5 个 Tab（首页/账单/统计/资产/我的）。

---

## 10. 安全机制说明

| 机制 | 实现位置 | 存储方式 | 强度/说明 |
|---|---|---|---|
| 应用隐私锁（PIN/图案） | `PrivacyLockStore` | `SharedPreferences`：仅存哈希+盐 | PBKDF2WithHmacSHA256，120,000 次迭代，256 位输出，32 字节 `SecureRandom` 盐；**不可逆**，忘记密码只能走"邮箱验证→重置"流程，无本地找回后门 |
| 邮箱授权码（用于发验证码的 SMTP 认证） | `EmailCredentialStore` | `AndroidKeyStore` 生成的 AES 密钥，AES/GCM/NoPadding 加密后存 `SharedPreferences` | 密钥本身不出设备（硬件级/系统级密钥库），IV 随机且随密文一起保存 |
| 邮箱验证码 | `EmailVerificationStore` | `SharedPreferences`：仅存 SHA-256 哈希 | 5 分钟过期、60 秒重发冷却、最多 5 次尝试超限锁定 |
| 邮箱基础配置（主机/端口/SSL 开关） | `EmailConfigStore` | `SharedPreferences` 明文 | 非敏感信息，无需加密 |
| 全量数据备份 | `BackupRepository` | 明文 JSON 文件（`filesDir/backup/`） | **备份文件本身未加密**，若设备被 root 或备份文件被导出，账单等财务隐私数据可被直接读取——建议后续视需求增加备份文件加密选项 |
| 系统级自动备份 | `AndroidManifest` | `allowBackup="false"` | 明确禁止 Google 自动云备份/adb backup，避免财务数据无意间流出设备 |

---

## 11. 已知问题 / 技术债 / 未完成占位

1. **7 个空占位类**（见 8.14 节）：`AccountActivity`/`BudgetActivity`/`CategoryActivity`/`LockActivity`/`BackupActivity`/`ImportBillActivity`/`AddBillBottomSheet`，均无内容且未注册。建议要么补全并接入，要么直接删除以减少认知负担；`activity_budget.xml` 布局也随之成为孤儿资源。
2. **两个空 utils 占位类**：`BackupManager`（真正实现在 `BackupRepository`，类名与职责错位，建议删除或重命名澄清）、`NaturalLanguageParser`（"自然语言记账"功能未实现，仅占位）。
3. **MineFragment 中的"即将接入"入口**：个人信息（`cardProfile`）、导出账单（`itemExport`）、意见反馈（`itemFeedback`）目前点击只弹 Toast，功能未实现。
4. **CSV 解析器与 XLSX 解析器的"中性交易"处理不一致**：`CsvBillParser` 直接丢弃非收入非支出的行；`XlsxBillParser` 会保留为 `NEUTRAL` 交由导入预览做转账识别。二者面向同一批"微信账单"场景却行为不同，建议统一到 XLSX 解析器的（更完整的）逻辑，或抽取公共的"中性交易判定"模块复用。
5. **无统一状态管理/跨页面自动刷新**：多数 Fragment 各自 `new FinanceRepository(requireContext())`，而非统一走 `MainViewModel`；一个页面改了账户/分类，其余已经打开的页面不会自动感知变化，依赖用户手动切回触发 `onResume`/`observeData` 重新拉取。
6. **JSON 全量读写无并发保护**：`JsonDataStore` 的每次增删改都是"整表读出→内存改→整表写回"，没有文件锁或事务保证；正常单线程 UI 操作下问题不大，但如果后续引入后台线程（如导入大文件时的批量写入）需要格外小心竞态。
7. **备份恢复非严格事务**：`BackupRepository.restoreBackup()` 对 6 个文件逐个"写临时文件→替换"，中途失败会 `throw` 并整体返回 `false`，但**已经替换成功的文件不会回滚**，理论上可能出现新旧数据混杂的中间态。
8. **国际化缺失**：几乎所有界面文案直接写死在 Kotlin/XML 中，`strings.xml` 仅有个位数字符串，暂不具备多语言能力。
9. **色值定义分散且略有出入**：`colors.xml` 中 `income`(#22A06B) 与 `income_color`(#35A66F)、`expense`(#E85D75) 与 `expense_color`(#E85D5D) 两套近似但不同的收支配色同时存在，代码中各处引用需人工核对是否统一。
10. **无自动化测试**：项目未见任何 `test`/`androidTest` 目录或单元测试代码；`FinanceCalculator`、`ImportDuplicateChecker`、`CsvBillParser`/`XlsxBillParser` 均是纯逻辑、无 Android 依赖，是最适合优先补单元测试的模块。
11. **`JsonDataStore` 异常处理未接入统一日志**：读写异常仅 `e.printStackTrace()`，未调用项目自带的 `AppLogger`，与 `ExcelExporter`/`CrashHandler` 等模块的日志规范不一致。
12. **`BackupData.kt` 未被实际使用**：该 data class 字段类型为 `Any?`，理论上是为了兼容 Gson 反序列化，但 `BackupRepository` 实际是直接用 `JsonObject`/`JsonElement` 手工拼装，并未使用这个 data class，属于死代码。

---

## 12. 后续开发建议

1. **收敛数据访问入口**：把所有 Fragment 对 `FinanceRepository` 的直接实例化收敛到 `MainViewModel`（或拆分为多个按领域划分的 ViewModel + 一个共享的 Repository 单例），从根本上解决"多页面数据不同步"的问题。
2. **抽取导入规则引擎**：把 `ImportPreviewFragment` 中账户自动匹配、分类自动匹配、去重判定等大段逻辑迁移到独立的、不依赖 Fragment 生命周期的 `ImportResolver`/`UseCase` 类中，便于编写单元测试与后续复用（比如未来要支持支付宝官方账单格式时）。
3. **统一"中性交易"（转账类）识别逻辑**，让 CSV 与 XLSX 两条导入路径行为一致。
4. **补充单元测试**，优先覆盖：`FinanceCalculator`、`CsvBillParser`/`XlsxBillParser`、`ImportDuplicateChecker`、`JsonDataStore` 的增删改查。
5. **清理死代码**：删除或补全 7 个空 Activity 占位类、`BackupManager`、`NaturalLanguageParser`、孤儿布局 `activity_budget.xml`、未使用的 `BackupData` data class。
6. **补齐 `MineFragment` 中"即将接入"的功能**（个人信息、导出账单入口、意见反馈），或先从 UI 上移除以免用户产生功能已完成的错觉。
7. **视需求评估备份文件加密**：当前本地备份 JSON 明文存储，如果未来支持备份文件导出到外部存储/云盘，建议增加密码保护或复用现有的 AES/Keystore 能力做加密备份选项。
8. **字符串资源化**：将硬编码中文文案迁移到 `strings.xml`，为未来可能的多语言支持打基础（即使当前无国际化需求，也有利于文案统一维护）。
9. **考虑引入 Room**：当账单量增长后，纯 JSON 全量读写的性能与可维护性会下降，Room + SQLite 可以在保持"完全离线"这一核心特性不变的前提下解决增长瓶颈；如暂不迁移，也建议为 `JsonDataStore` 增加简单的内存缓存层减少重复读文件。

---

## 13. 全量类清单速查表

> 按包路径逐一列出全部 82 个 Kotlin 文件（含空占位类），供检索。

### `com.example.myno.jz`
- `MoneyBookApplication` — Application 入口，初始化 AppLogger/CrashHandler

### `data.model`
- `Account`, `AccountType`
- `AppSettings`, `LockType`, `ThemeMode`, `DefaultBillType`, `AfterAddAction`, `DateFormatType`, `DecimalPlaces`, `StatisticsPeriod`
- `BackupData`（未实际使用）
- `Bill`, `BillType`
- `Budget`, `BudgetType`
- `Category`, `CategoryType`
- `EmailConfig`
- `EmailProvider`
- `EmailVerification`
- `PrivacyLockConfig`, `PrivacyLockType`
- `Transfer`

### `data.local`
- `DefaultDataInitializer` — 默认账户/分类播种
- `JsonDataStore` — 6 类业务对象的通用 JSON CRUD

### `data.repository`
- `BackupRepository` — 本地全量备份/恢复
- `EmailConfigStore` — 邮箱配置存储
- `EmailCredentialStore` — 邮箱授权码 Keystore 加密存储
- `EmailVerificationStore`, `VerificationResult` — 邮箱验证码校验
- `FinanceRepository` — 业务数据门面
- `PrivacyLockStore` — 隐私锁 PBKDF2 存储

### `data.imports`
- `CsvBillParser` — CSV/TXT/微信 CSV 解析
- `ImportBillItem`, `ImportBillType` — 导入条目模型
- `ImportDuplicateChecker` — 导入去重
- `XlsxBillParser` — XLSX/微信 XLSX 解析（手写 OOXML 读取）

### `data.email`
- `EmailConnectionTester` — IMAP/SMTP 连接测试与验证码发送（Jakarta Mail）

### `utils`
- `AppLogger` — 文件日志
- `BackupManager`（空占位）
- `CrashHandler` — 全局崩溃捕获
- `ExcelExporter`, `ValidationResult` — 自研 XLSX 导出与校验
- `FinanceCalculator` — 日期/收支统计纯函数
- `NaturalLanguageParser`（空占位）

### `ui.account`
- `AccountActivity`（空占位）

### `ui.addbill`
- `AddBillBottomSheet`（空占位）

### `ui.assets`
- `AccountDetailFragment`
- `AccountFlowAdapter`, `AccountFlowFragment`, `AccountFlowItem`
- `AccountSettingsFragment`
- `AddAccountFragment`
- `AssetsFragment`
- `EditTransferFragment`
- `TransferDetailFragment`
- `TransferFragment`

### `ui.backup`
- `BackupActivity`（空占位）
- `BackupFragment`
- `BackupViewModel`

### `ui.bills`
- `AddBillFragment`
- `BillAdapter`
- `BillDetailFragment`
- `BillsFragment`
- `ImportBillFragment`
- `ImportPreviewAdapter`
- `ImportPreviewFragment`

### `ui.budget`
- `BudgetActivity`（空占位）
- `BudgetAdapter`
- `BudgetManageFragment`
- `BudgetViewModel`

### `ui.category`
- `CategoryActivity`（空占位）
- `CategoryManageAdapter`
- `CategoryManageFragment`

### `ui.common`
- `FragmentNavigation`（`FragmentManager.openScreen` 扩展函数）

### `ui.email`
- `EmailConfigFragment`
- `EmailVerificationFragment`

### `ui.home`
- `CategoryDonutChartView` — 自绘环形图
- `HomeFragment` — 首页（全项目最大文件）
- `QuickAction`
- `QuickActionAdapter`
- `QuickActionStore`
- `RecentBillAdapter`

### `ui.importbill`
- `ImportBillActivity`（空占位）

### `ui.lock`
- `LockActivity`（空占位）

### `ui.main`
- `MainActivity`
- `MainViewModel`

### `ui.mine`
- `MineFragment`

### `ui.privacy`
- `ChangePasswordFragment`
- `ChangePatternFragment`
- `ChangeSecurityFragment`
- `PasswordSetupHelper`
- `PatternLockView` — 自绘图案锁
- `PatternSetupHelper`
- `PrivacyLockFragment`
- `PrivacyLockVerifyFragment`
- `ResetPasswordFragment`
- `ResetPatternFragment`

### `ui.quick`
- `QuickEntryActivity`

### `ui.settings`
- `SettingsFragment`

### `ui.statistics`
- `StatisticsFragment`（MPAndroidChart 饼图+趋势图，全项目第二大文件）

---

*文档完。如代码后续有变更，建议同步更新本文件对应章节，或让 AI 助手基于最新代码重新生成第 8、11、13 节。*
