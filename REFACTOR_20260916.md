# MoneyBook 2026-09-16 代码核对与全量瘦身记录

## 一、目标

本次不是通过“删除文件”制造表面精简，而是在**不破坏现有功能、不删除未完成功能**的前提下，减少重复代码、重复 XML 属性和编辑器缓存，并把真实项目结构重新记录到全部 MD 文档中。

## 二、已完成的优化

### 第一阶段：通用结构整理
- 删除 `.acside` 编辑器缓存/符号缓存，不参与 Android 构建。
- 新增 `ui/common/FragmentNavigation.kt`，统一 Fragment transaction 模板。
- 整理 `MineFragment` 的重复导航代码与邮箱验证结果处理。
- 重做 `fragment_mine.xml` 的重复卡片结构为更紧凑的功能分组，同时保留原有 binding ID。
- 保留所有潜在未完成功能，不删除空壳类。

### 第二阶段：设置页与资源层瘦身
- `SettingsFragment.kt`：主题、动画、默认记账类型、记账后行为、日期格式、金额小数位、统计周期等单选弹窗统一到 `showChoiceDialog()`。
- `fragment_settings.xml`：9 个设置 Row 统一使用 `MoneyBookSettingsRow`。
- `fragment_settings.xml`：5 个设置 Card 统一使用 `MoneyBookSettingsCard`。
- `values/styles.xml`：新增 `MoneyBookSettingsCard`，收敛重复 margin/corner/elevation 等属性。
- drawable 全项目静态引用扫描：没有发现能够“确定安全删除”的资源，因此本次没有删除 drawable。

## 三、明确保留的未完成功能

| 文件 | 状态 | 处理原则 |
|---|---|---|
| `utils/ExcelExporter.kt` | 空类 | 保留，后续实现 Excel 导出 |
| `utils/NaturalLanguageParser.kt` | 空类 | 保留，后续实现自然语言记账 |
| `utils/BackupManager.kt` | 空类 | 保留，避免破坏既有备份设计 |
| `ui/category/CategoryActivity.kt` | 空壳 | 保留，未注册 Manifest；真实功能在 Fragment |
| `ui/budget/BudgetActivity.kt` | 空壳 | 保留，未注册 Manifest；真实功能在 Fragment |
| `ui/backup/BackupActivity.kt` | 空壳 | 保留，未注册 Manifest；真实功能在 Fragment |
| `ui/lock/LockActivity.kt` | 空壳 | 保留，未注册 Manifest；真实锁屏在 PrivacyLockVerifyFragment |

## 四、当前功能状态

- 分类管理：已完成
- 预算管理：已完成
- 统计：已完成
- 隐私锁 PIN/图案：已完成
- 安全邮箱验证/找回：已完成
- 首页快捷操作 + 桌面快捷方式：已完成
- 默认账户/分类/记账类型/记账后行为：已完成
- 备份恢复：部分完成，目前仅 `records.json`
- Excel 导出：未实现
- 自然语言记账：未实现
- 日期格式/金额小数位：设置端已保存，显示层尚未全局应用

## 五、当前完整代码树

以实际优化工作目录为准，完整 Kotlin 树见 `MoneyBook_项目完整开发记录.md` 第 5.1 节。当前核心结构：

```text
com/example/myno/jz/
├── data/
│   ├── email/
│   ├── imports/
│   ├── local/
│   ├── model/
│   └── repository/
├── ui/
│   ├── account/
│   ├── addbill/
│   ├── assets/
│   ├── backup/
│   ├── bills/
│   ├── budget/
│   ├── category/
│   ├── common/
│   ├── email/
│   ├── home/
│   ├── importbill/
│   ├── lock/
│   ├── main/
│   ├── mine/
│   ├── privacy/
│   ├── quick/
│   ├── settings/
│   └── statistics/
└── utils/
```

资源目录核对：`layout 40`、`drawable 50`、`color 4`、`menu 1`、`values 4`、`values-night 2`、`xml 3`，另有 launcher/mipmap 资源。

## 六、验证结果

### 已通过
- Android XML 全量标准解析。
- Kotlin 大括号结构检查。
- 静态资源引用扫描。
- 文档树与当前文件系统重新核对。

### 未完成
- `./gradlew :app:assembleDebug --no-daemon` 无法在当前环境完成，因为 Gradle Wrapper 需要从 `services.gradle.org` 获取 Gradle 9.0.0，而当前环境无网络/DNS 访问。
- 因此本次结果不能标记为“Android 编译通过”。

## 七、下一步

1. 先在本地可编译环境验证当前瘦身版。
2. 然后按模块继续：备份恢复 → Excel → 自然语言 → 日期/小数位全局应用。
3. 功能补齐后，再对 Home / Bills / ImportPreview / Statistics / EmailConfig 做深层职责拆分。
4. 每完成一个阶段，都重新更新全部项目 MD 文档及代码树。
